import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  switchTrackActive: {
    color: "#000",
  },
  switchTrackInactive: {
    color: "#21ffb5",
  },
});

export const colors = {};

export default styles;
