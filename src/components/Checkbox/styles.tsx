import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    marginTop: 20,
  },
  checkbox: {
    color: "#50B5FF",
  },
  label: {
    marginLeft: 5,
    color: "#696974",
  },
});

export default styles;
