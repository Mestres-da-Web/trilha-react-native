import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    height: 200,
  },
  componentContainer: {
    borderWidth: 1,
    margin: 10,
    height: 50,
  },
});

export default styles;
